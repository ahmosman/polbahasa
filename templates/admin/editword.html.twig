{% extends 'admin/word.html.twig' %}

{% block title %}Edytuj {{ word.name }}{% endblock %}

{% block edit_word_section %}
    <section class="edit-word-section">
        <div class="header-word">{{ word.name }}</div>
        {% for speechSection in speechSections %}
            {% if speechSection is not empty %}
                <div class="speech-section">
                    <div class="part-of-speech-div">
                        <span class="part-of-speech-span">
                            <input class="part-of-speech" value="{{ speechSection[0].partOfSpeech }}">
                        </span>
                    </div>
                    <ol class="foreign-word-ol">
                        {% for meaning in speechSection %}
                            <li class="foreign-meaning-li" id="{{ meaning.id }}">
                                <div class="foreign-meaning-name">
{% for meaningName in meaning.meaningNames %}{{ meaningName.name }}{% if loop.index < meaning.meaningNames|length %}, {% endif %}{% endfor %}</div>
                                {% for example in meaning.examples %}
                                    <div class="sentence-section">
                                        <div class="example-sentence">{{ example.sentence }}</div>
                                        <div class="example-translation">{{ example.translation }}</div>
                                    </div>
                                {% endfor %}
                            </li>
                        {% endfor %}
                    </ol>
                </div>
            {% endif %}
        {% endfor %}
    </section>
{% endblock %}
